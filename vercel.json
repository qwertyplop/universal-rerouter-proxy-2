{
  "version": 2,
  "builds": [
    { "src": "api/proxy.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(janitorai|sillytavern|health|api/proxy)(/.*)?", "dest": "api/proxy.py" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
