[project]
name = "universal-rerouter-proxy"
version = "0.1.0"
description = "Universal Rerouter Proxy"
requires-python = ">=3.12"
dependencies = [
    "requests==2.32.3",
]

[tool.vercel]
entrypoint = "api.proxy:app"
